# API root — no version segment; every call names /v1 or /v2 explicitly.
VITE_API_URL=http://localhost:3333

# Parent domain for the aide_token cookie, so *.aide.app shares one session.
# Leave empty locally; set to aide.app in deployed environments.
VITE_COOKIE_DOMAIN=
